# Q: pytest 9.0 errors instead of skipping when `unittest.SkipTest` is raised
**Source:** https://github.com/pytest-dev/pytest/issues/13895

## The Problem / Request
This code:

```
import unittest

def test_skiptest():
    raise unittest.SkipTest()
```

had this behavior on pytest <9.0:

```
$ rm venv -r
$ python3 -m venv ./venv
$ ./venv/bin/pip install "pytest<9"
[...]
Successfully installed iniconfig-2.3.0 packaging-25.0 pluggy-1.6.0 pygments-2.19.2 pytest-8.4.2
$ ./venv/bin/pytest test.py
=== test session starts ===
platform linux -- Python 3.13.5, pytest-8.4.2, pluggy-1.6.0
rootdir: /home/dev-irc
collected 1 item
test.py s        [100%]

=== 1 skipped in 0.03s ===
```

and this on pytest 9.0.0:

```
$ rm venv -r
$ python3 -m venv ./venv
$ ./venv/bin/pip install "pytest==9.0.0"
[...]
Successfully installed iniconfig-2.3.0 packaging-25.0 pluggy-1.6.0 pygments-2.19.2 pytest-9.0.0
$ ./venv/bin/pytest test.py
=== test session starts ===
platform linux -- Python 3.13.5, pytest-9.0.0, pluggy-1.6.0
rootdir: /home/dev-irc
collected 1 item
test.py F       [100%]

==== FAILURES ===
____ test_skiptest ____

    def test_skiptest():
>       raise unittest.SkipTest()
E       unittest.case.SkipTest

test.py:3: SkipTest
=== short test summary info ===
FAILED test.py::test_skiptest - unittest.case.SkipTest
=== 1 failed in 0.06s ===
```

Python version: 3.13.5

## The Solution / Discussion
### Response 1
Thanks for the report.

Bisected to a620d24376eb2c4bc964f2b6efcc694a4adbbe21 (@nicoddemus).

However, you raising a unittest exception in a non-unittest test, is there a reason for that? Can you use `pytest.skip()` instead?

### Response 2
Just to clarify my comment, raising `unittest.SkipTest` still works inside a unittest `TestCase`.

### Response 3
It's inherited from before I used pytest.

I actually use subclasses of `unittest.SkipTest` and I don't think I ever try to catch them in an `except` block so I should be able to convert them to functions wrapping `pytest.skip()`. But I imagine someone else might not.

### Response 4
Also, I have people not familiar with pytest on this codebase, and `raise MySkipTestSubclass()` probably makes the control flow clearer for them than calling `my_pytestdotskip_wrapper()`

### Response 5
Looking at the commit, this worked before due to nose support, which has since then been removed.

I don't think we should continue to support this usage, TBH. Seems simple enough for users to adapt this code to use `pytest.skip()` instead.

What do you folks think?

### Response 6
@bluetech 

> raising unittest.SkipTest still works inside a unittest TestCase.

That is not the case for me now in 9.0.0. 

Raising `unittest.SkipTest` inside of `unittest.TestCase` is resulting in pytests reporting an error instead of a skip.

```
class TestFoo(unittest.TestCase):
@classmethod
    def setUpClass(cls):
        if cls is Bar:
            raise unittest.SkipTest('Skip Bar tests as it is a base class')

### Response 7
@nicoddemus 

Please see my comment above. `unittest.SkipTest` is broken for me now when using it in a `unittest.TestCase`. 

Thank you.

### Response 8
> Please see my comment above. unittest.SkipTest is broken for me now when using it in a unittest.TestCase.

Oh if that's the case then definitely this is a problem. I will look into it, thanks!

### Response 9
Yeah, chiming in from a project where pytest is entirely optional, everything is defined with pure unittest and people can run pytest over it if they want. The skips result in errors now when using pytest, but not when using the python included unittest runner.

### Response 10
Being able to `raise unittest.SkipTest()` without worrying about whether my users are running pytest or unittest is nice for other testing libraries like Hypothesis too, so I'd support restoring the old behavior 🙂 

### Response 11
Yeah, I hit this [here](https://app.circleci.com/pipelines/github/simplistix/testfixtures/2535/workflows/16fd34ec-aa01-4cd2-bde2-b476be8811bc/jobs/45225), which is using Sybil's [skip parser](https://sybil.readthedocs.io/en/latest/rest.html#skipping-examples) ([code](https://github.com/simplistix/sybil/blob/master/sybil/evaluators/skip.py#L3)).
There's a weird thing in that error, whereby it looks like importing twisted ends up with unittest's `SkipTest` being replaced by `twisted.trial.unittest.SkipTest` just by way of importing `twisted` at some point, but I reproduced the problem folks are reporting above even with it uninstalled.

I guess my feedback from a library maintainer is to please generously support `SkipTest` exceptions if at all possible 😓 

### Response 12
Having the same issue with `@unittest.skip()` decorator

### Response 13
having the same issue.
```python
def test_set_synapse_dynamics() -> None:
    raise unittest.SkipTest("needs fixing")
```
using github actions
works with  pytest 8.4.2 not 9.0.0

### Response 14
Is there an ETA for this fix to be released as 9.0.1? Some of our test automation pipelines have been failing since the release of pytest 9.0.0 3 days ago.

### Response 15
Soon. 👍 

In fact we were about to release 9.0.1 today but ran into some problems: https://github.com/pytest-dev/pytest/pull/13928. Thanks to @webknjaz the solution was found, I will trigger a new build shortly.

