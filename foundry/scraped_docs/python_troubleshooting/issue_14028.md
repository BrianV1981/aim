# Q: Expose FixtureRequest publicly
**Source:** https://github.com/pytest-dev/pytest/issues/14028

## The Problem / Request
#### What's the problem this feature will solve?
Allow typehinting of the request (FixtureRequest) input parameter, without importing from the private `_pytest` module.

#### Describe the solution you'd like
expose `FixtureRequest` publicly (it is now stored in  `_pytest.fixture.FixtureRequest`)

#### Alternative Solutions
I believe the only solution for now is importing from the private `_pytest` module or skipping the type hint.


## The Solution / Discussion
### Response 1
https://github.com/pytest-dev/pytest/blob/8b3550c6cb4ea3898140df153a83addc8cd65710/src/pytest/__init__.py#L28

