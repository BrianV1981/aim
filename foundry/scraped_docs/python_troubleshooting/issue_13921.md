# Q: Versions 8.4.x and 9.0.x missing on ReadTheDocs
**Source:** https://github.com/pytest-dev/pytest/issues/13921

## The Problem / Request
This is the current selection, which is after the 8.4 and 9.0 releases:

<img width="337" height="343" alt="Image" src="https://github.com/user-attachments/assets/3c1f9914-877e-42b2-8a74-c759e0907d80" />

I think it needs to be added to the RTD project settings:
https://app.readthedocs.org/projects/pytest/
I don't have access myself so another core dev will need to do it.

Also we should probably add this step to RELEASING.rst, or maybe it's possible to automate.

## The Solution / Discussion
### Response 1
I have access to readthedoc but, I'm not sure what I should do. On other repositories the tag are automatically added to this selector and I remove patch versions when the last one is released...

### Response 2
I ReadThisDoc: https://docs.readthedocs.com/platform/stable/versions.html

I think what is needed is to configure the 8.4.x and 9.0.x as "active".

If I'm reading correctly all branches/tags are not active by default, so it's interesting that it's done automatically in your other repos. Maybe because we use branches vs. tags?

### Response 3
Yes, I think this is exactly due to branches vs. tags.

### Response 4
Must use "Add Version" on the top right, choose the branch, then set the new version as active.

<img width="1154" height="396" alt="Image" src="https://github.com/user-attachments/assets/7240f1ee-bfc1-423e-8b4a-1bda1f5148b0" />

I think it's going to work once the build is done.

### Response 5
Thanks @Pierre-Sassoulas, I submitted a PR to add what you wrote to RELEASING: #13949

