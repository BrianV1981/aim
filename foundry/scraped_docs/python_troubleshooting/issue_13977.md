# Q: I'd like to ask if pytest test cases support dynamically modifying the scope of fixtures during runtime.
**Source:** https://github.com/pytest-dev/pytest/issues/13977

## The Problem / Request
I'd like to ask if pytest test cases support dynamically modifying the scope of fixtures during runtime.

## The Solution / Discussion
### Response 1
please outline the use-case - theres ways to have specific configruations be more accomodating but theres no general/internal support

### Response 2
The current scope of my fixture is at the function level. However, in certain situations, I would like for the fixture's scope to be at the module level so that all tests within the file can share the same fixture.

### Response 3
You'd need to be a bit less abstract if you want a good answer. You can pass a callable to `scope` which will get a pytest config object as an argument and can return a scope.

In any case, converting this to a discussion as this is nothing actionable for pytest.

