# Q: Native `pyproject.toml` configuration
**Source:** https://github.com/pytest-dev/pytest/issues/13743

## The Problem / Request
#### What's the problem this feature will solve?
<!-- What are you trying to do, that you are unable to achieve with pytest as it currently stands? -->

Provide native support for toml configuration to avail of rich types (and associated editor support), improve alignment across Python tooling; and remove a dependency on two configuration formats (toml & ini) within projects.

#### Describe the solution you'd like
<!-- A clear and concise description of what you want to happen. -->

<!-- Provide examples of real-world use cases that this would enable and how it solves the problem described above. -->

Native toml support with a standalone `pytest.toml` and a `[tool.pytest]` section in `pyproject.toml` ([PEP-518](https://peps.python.org/pep-0518/#tool-table)) - with the latter outlined as the intent within [pytest configuration documentation](https://docs.pytest.org/en/7.1.x/reference/customize.html#pyproject-toml).

> One might wonder why `[tool.pytest.ini_options]` instead of `[tool.pytest]` as is the case with other tools
>
> The reason is that the pytest team intends to fully utilize the rich TOML data format for configuration in the future, reserving the `[tool.pytest]` table for that. The ini_options table is being used, for now, as a bridge between the existing .ini configuration system and the future configuration format.

#### Alternative Solutions
<!-- Have you tried to workaround the problem using a pytest plugin or other tools? Or a different approach to solving this issue? Please elaborate here. -->

Use INI configuration embedded within TOML through the `[tool.pytest.ini_options]` section within `pyproject.toml`.

#### Additional context

- https://github.com/pytest-dev/pytest/issues/1556
- https://github.com/pytest-dev/pytest/issues/3523
- https://github.com/pytest-dev/pytest/issues/3675
- https://github.com/pytest-dev/pytest/issues/13173
- `tox` implementation 1 October 2024 (https://github.com/tox-dev/tox/issues/999) - migrating from a `legacy_tox_ini` key within a `[tool.tox]` section. Note: yet to have full parity with all capabilities provided by the ini alternative.

## The Solution / Discussion
### Response 1
Hopefully this is not too difficult, I will try to implement this when I get a chance.

As mentioned in #12228, when we do this, we should make sure to document the toml type for each configuration key in the API reference. Maybe even create an automatically generated schema thingy that can be understood by IDEs.

> Native toml support with a standalone pytest.toml

I don't think we discussed `pytest.toml`, but I think it makes sense to add along with `[tool.pytest]` in pyproject.toml. Some projects prefer to use separate configuration files for separate tools instead of all in pyproject.toml and that's entirely sensible. It's unfortunate that we would have to add yet-another-configuration-file-name but what can you do...

### Response 2
Im experimenting with a possible solution in cot.config.ingest



### Response 3
> Im experimenting with a possible solution in cot.config.ingest

What is cot.config.ingest?

In any case, until that is ready I think we can implement this in pytest itself, the code changes are not really hard, the main effort is the documentation which we'll need to do anyway.

### Response 4
I worked on this for a few hours this morning, draft PR here: #13846. Not complete yet but almost done.

### Response 5
How does a user continue to use minversion, given its now moved positions and won't be read by older versions?

### Response 6
There is no way to do it I'm afraid. That's a problem with introducing a new configuration file.

Although I wonder if there's a real use case for `minversion` these days, it seems better to restrict pytest in the pyproject.toml/requirements.txt/distro package level.

### Response 7
Is it possible, if and only if reading from tool.pytest, to continue to read tool.pytest.ini_options.minversion if tool.pytest.minversion is not present? 

