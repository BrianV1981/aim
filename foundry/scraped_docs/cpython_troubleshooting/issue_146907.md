# Q: Documentation about debug and release build ABI compatibility is misleading
**Source:** https://github.com/python/cpython/issues/146907

## The Problem / Request
### Documentation

The documentation on the ABI compatibility says:

https://github.com/python/cpython/blob/7b2495e8111ee316fc9a781eb7de4eaac681b379/Doc/using/configure.rst?plain=1#L908-L911

However, this isn't ABI compatibility in the general sense, it's a one-way compatibility: An extension module built against a release build can run on a debug build, but an extension module built against a debug build may use symbols that are only defined in a debug build.

<!-- gh-linked-prs -->
### Linked PRs
* gh-146925
* gh-147971
* gh-147972
<!-- /gh-linked-prs -->


## The Solution / Discussion
### Response 1
@vstinner, is this intentional, or a bug?

### Response 2
> An extension module built against a release build can run on a debug build, but an extension module built against a debug build may use symbols that are only defined in a debug build.

That's correct. The documentation should be updated to clarify that.

